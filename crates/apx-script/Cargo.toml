[package]
name = "apx-script"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Aperture X — Skript-API (Phase 9 Schritt 9, siehe PLAN.md, DECISIONS.md ADR-0035 Punkt 3): eine schmale, primitiv-typisierte Rhai-Brücke auf die Grundeinstellungs-Regler (get_exposure()/set_exposure(f64) statt ganze EDL-Structs durchzureichen). rhai ist reines Rust und sandboxbar (Operationslimits) — eine bessere Wahl als Lua-Bindings, die einen C-Interpreter bräuchten."

[dependencies]
apx-core.workspace = true
apx-pipeline.workspace = true
thiserror.workspace = true
rhai = "1"
